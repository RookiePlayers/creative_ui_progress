name: creative_ui_progress
description: "A Flutter package providing beautiful, animated, and highly customizable progress indicators."
version: 0.1.0
homepage: "https://github.com/RookiePlayers/creative_ui_progress.git"

environment:
  sdk: ">=2.19.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
